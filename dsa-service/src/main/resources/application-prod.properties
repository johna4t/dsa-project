
# Placeholders to be replaced with with real prod credentials when ready.
# Use ddl-auto=validate or none in prod to avoid schema changes.
spring.datasource.url=jdbc:postgresql://<your-production-db-host>:5432/dsa
spring.datasource.username=<your-prod-username>
spring.datasource.password=<your-prod-password>
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.show-sql=false